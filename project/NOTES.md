Configuración inicial y revisión de dependencias completada. Las dependencias principales para el sistema de reservas ya están presentes en los archivos package.json.

Observaciones:
- Se ha añadido "@tanstack/react-query": "^5.0.0" a las dependencias del frontend en `project/package.json`.
